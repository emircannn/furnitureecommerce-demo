// ============================================================
// Belenay Mobilya - PostCSS Yapılandırması
// Tailwind CSS v4 ile @tailwindcss/postcss kullanılır
// ============================================================

/** @type {import('postcss-load-config').Config} */
const config = {
  plugins: {
    // Tailwind CSS v4 PostCSS eklentisi
    "@tailwindcss/postcss": {},
  },
};

export default config;
